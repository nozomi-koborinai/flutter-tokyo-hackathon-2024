---
model: googleai/gemini-1.5-flash-latest
config:
  temperature: 0.7
input:
  schema:
    prompt: string
output:
  format: json
  schema:
    translatedPrompt: string
---

Translate the following {{prompt}} into English while preserving its meaning.